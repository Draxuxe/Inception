void plus_jeune_plus_vieux(struct truc list(change aussi ici), int &jeune, int &vieux)
{
    for (int i=0; i<TAILLE_LISTE(change cette valeur); i++)
    {
        if (list.tab[i](change aussi ici) < jeune)
            jeune = list.tab[i](change la);
        if (list.tab[i](change aussi ici) > vieux)
            vieux = list.tab[i](change la);
    }
}

int main()
{
    int jeune=0;
    int vieux=0;
    struct truc struct;

    plus_jeune_plus_vieux(struct, jeune, vieux);
}