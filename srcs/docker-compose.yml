version: '3'

services:
  nginx:
    build: ./requirements/nginx
    image: nginx
    container_name: nginx
    ports: 
      - 443:443
    restart: on-failure
    networks:
      - inception

networks:
  inception:
    driver: bridge